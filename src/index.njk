{% extends '_templates/base.njk' %}

{% block page_title %}2024 ICJ Final Project{% endblock %}

{% block page_description %}The index search description for this page goes here.{% endblock %}

{% block content %}
<div class="container">
  <h1>Johan's Likes & Dislikes</h1>
  <p>I'm a senior journalism major at the University of Texas at Austin. I'm a very opinionated person. So, I will like to share some of my like and dislikes with whoever reads this. Let me know through the "Contact Me" form if you agree with my opinions or not :)</p>
  <h2>Likes Loop</h2>
  <p>This loops through data that came from a Google sheet example.</p>
  <div class="row">
    {% for book in library.books %}
      <div class="col">
        <img src="img/{{ book.slug }}.jpg" alt="" class="img-fluid">
        <h3><a href="books/{{ book.slug }}.html">{{ book.title }}</a></h3>
      </div>
    {% endfor %}
  </div>
  <h2>Bookstores loop</h2>
  <p>This loops through data that came from a Google Sheet example.</p>
  <hr>
  {% for store in bookstores.stores %}
    <h3><a href="{{ store.url }}" target="_blank">{{ store.name }}</a></h3>
    <p>{{ store.address}}, {{ store.city }}, {{ store.state }} {{ store.zip }}</p>
    <hr>
  {% endfor %}
</div>
{% endblock %}